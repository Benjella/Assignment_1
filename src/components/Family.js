// src/components/Family.js
import React from 'react';
import './Family.css';

const Family = () => {
    return (
        <section id='family' className="family">
            <h2>Family</h2>
            <p>I belong to a family of four with an older sibling ,mother and father.</p>
        </section>
    );
};

export default Family;
